# PostgreSQL connection string (replace with your actual credentials in .env)
DATABASE_URL="postgresql://your-username:your-password@your-neon-host.neon.tech/your-database-name?sslmode=require"

# Base path for frontend to call backend APIs
NEXT_PUBLIC_API_BASE="/api"

# Port used for running the backend server locally
PORT=3000
